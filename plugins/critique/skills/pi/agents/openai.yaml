interface:
  display_name: "Pi Critique"
  short_description: "Get an independent review from the pi coding agent"
  default_prompt: "Use $pi to review the referenced file and summarize the most important findings."
policy:
  allow_implicit_invocation: false
