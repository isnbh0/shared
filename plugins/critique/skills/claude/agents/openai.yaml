interface:
  display_name: "Claude Critique"
  short_description: "Get an independent review from Claude Code"
  default_prompt: "Use $claude to review the referenced file and summarize the most important findings."
policy:
  allow_implicit_invocation: false
