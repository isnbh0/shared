# Macros configuration
# Copy this file to .claude/skill-configs/macros/config.yaml (project scope)
# or .claude/skill-configs/macros/config.local.yaml (local/personal scope)

# Directory where mapreduce reports and intermediate artifacts are stored
# Relative paths are resolved from the current working directory
workspace_dir: .agent-workspace/macros
