# Spex configuration
# Copy this file to .agents/skill-configs/spex/config.yaml (project scope)
# or .agents/skill-configs/spex/config.local.yaml (local/personal scope)

# Directory where spec files are created and managed
# Relative paths are resolved from the current working directory
workspace_dir: .agent-workspace/specs
