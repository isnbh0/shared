"""Benchmark environments package.

STUB STATE (Phase 1): empty — no envs are registered yet. Phase 4 REPLACES this module
so that importing the package registers `timestamp` and `spex_write_phased` (each env
package calls `skillopt_oauth.registry.register_env` at import time).
"""
from __future__ import annotations
